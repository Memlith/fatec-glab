spring.application.name=glab

spring.data.mongodb.uri=${MONGO_URL}
server.port=${BACKEND_SERVER_PORT}
firebase.credentials.base64=${FIREBASE_CREDENTIALS_BASE64}

server.error.include-stacktrace=never
